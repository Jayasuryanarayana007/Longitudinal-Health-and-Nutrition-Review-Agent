# Server Configuration
PORT=5000
NODE_ENV=development

# Optional Gemini API Key for Server-Side AI Generation
# (If unset, system automatically uses smart local RAG fallback)
GEMINI_API_KEY=your_gemini_api_key_here
